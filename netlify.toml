[build]
  command = "npm run build"
  functions = "functions"
  publish = "dist"

[dev]
  framework = "#custom"
  command = "npm run dev"
  targetPort = 8080
  prot = 8888
  publish = "dist"
  autoLaunch = false